---
- name: Deploy inpx-web + liberama
  hosts: inpx_web_hosts
  become: true
  vars_files:
    - ../../vault/secrets.yml
  pre_tasks:
    - ansible.builtin.set_fact:
        ca_ready: "{{ step_ca_root_cert_pem | length > 0 }}"
  roles:
    - role: inpx_web
      when: ca_ready

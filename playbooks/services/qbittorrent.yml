---
- name: Deploy qBittorrent
  hosts: qbittorrent_hosts
  become: true
  vars_files:
    - ../../vault/secrets.yml
  pre_tasks:
    - ansible.builtin.set_fact:
        ca_ready: "{{ step_ca_root_cert_pem | length > 0 }}"
  roles:
    - role: qbittorrent
      when: ca_ready

---
- name: Deploy Traefik
  hosts: traefik_hosts
  become: true
  tags: [traefik]
  vars_files:
    - ../../vault/secrets.yml
  vars:
    step_ca_port: 8443   # fallback default; SOT in services/step_ca/defaults/main.yml
  roles:
    - role: traefik

---
- name: Register Traefik dashboard CNAME
  ansible.builtin.include_role:
    name: common
    tasks_from: register_cname
  vars:
    cname_name: "traefik.{{ inventory_hostname }}.{{ lan_domain }}"
    cname_target: "{{ inventory_hostname }}.{{ lan_domain }}"
